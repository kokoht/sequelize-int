<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title> seq association </title>
    <%- include partials/menubar %>
  </head>
  <body>
    <h1> Add Subject to Student </h1>
    <br>
    <br>
    <table>
      <tr>
        <th>Student Id</th>
        <th>First Name </th>
        <th>Last Name </th>
        <th>Email  </th>
        <th>Jurusan  </th>
      </tr>

      <tr>
        <td><%= panggilData.id %></td>
        <td><%= panggilData.first_name %></td>
        <td><%= panggilData.last_name %></td>
        <td><%= panggilData.email %></td>
        <td><%= panggilData.jurusan  %></td>
      </tr>
    </table>

<form action="/students/<%= panggilData.id%>/addsubject" method="post">
  <select name="SubjectId">
    <% panggilDataDropdown.forEach( subject => {%>
      <option value="<%= subject.id%>"><%= subject.subject_name%></option>
    <%})%>
  </select>

        <button type="submit"> submit </button>
      </form>
    </body>
  </html>
